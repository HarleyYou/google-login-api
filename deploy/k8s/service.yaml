apiVersion: v1
kind: Service
metadata:
  name: google-login-api-service
  namespace: default
spec:
  selector:
    app: google-login-api
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
